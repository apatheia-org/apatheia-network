package org.apatheia.network.algorithm.findvalue

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class PublisherFindValueAlgorithmSpec extends AnyFlatSpec with Matchers {

  behavior of "PublisherFindValueAlgorithm"

  it should "return the data for a particular target node id with a single request iteration" in {}

  it should "return the data for a particular target node id with multiple request iterations" in {}

  it should "return NOT FOUND error if no data is found for a target node id" in {}

  it should "not go more iterations than necessary" in {}

}
